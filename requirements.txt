# Laptop-side deps for the LOCAL track (src/) and to drive the AgentCore deploy.
# Python 3.10-3.12 (3.14 ships noisy / unsupported wheels -- avoid). Versions are
# pinned to the verified working set; see DESIGN_MEMO for the production-readiness
# rationale for pinning.

# Strands agent + tools (local track)
strands-agents==1.44.0
boto3==1.43.36

# Observability: LOCAL PHOENIX is the default backend (Step 3).
arize-phoenix==17.9.0                          # phoenix serve + span store
arize-phoenix-client==2.9.0                    # phoenix.client: pull spans, log annotations, datasets
pydantic-ai-slim==1.107.0                      # transitive dep of arize-phoenix; pin <2 (2.0.0 removed pydantic_ai.mcp.MCPServerStreamableHTTP and breaks `import phoenix`)
openinference-instrumentation-strands-agents==0.1.3
openinference-semantic-conventions==0.1.30     # SpanAttributes / DocumentAttributes for the manual span

# OpenTelemetry SDK + exporters (shared). HTTP exporter for local Phoenix,
# gRPC exporter is used by the AgentCore container path.
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-exporter-otlp-proto-http==1.42.1
opentelemetry-exporter-otlp-proto-grpc==1.42.1

# Tool structured output + experiments + evals
pydantic==2.13.4                               # RetrievalResult structured tool output
pandas==3.0.3                                  # span dataframe + metrics report
pyarrow==24.0.0                                # parquet export of spans
openai==2.43.0                                 # LLM-as-judge backend (Step 5)

# ---- Optional: only needed for the paths noted (not required for the core
# ---- local trace -> eval -> feedback demo). Pinned to a known-good resolve.

# Reference architecture diagram, only to regenerate docs/architecture.png
matplotlib==3.10.9

# Enterprise upgrade path (Arize AX): only needed if TRACE_BACKEND=ax
arize-otel==0.13.0

# Bedrock AgentCore deploy (TRACK B): only needed to run agentcore/deploy.py
bedrock-agentcore==1.15.0
bedrock-agentcore-starter-toolkit==0.3.9
