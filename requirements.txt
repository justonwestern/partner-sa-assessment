# Laptop-side deps for the LOCAL track (src/) and to drive the AgentCore deploy.
# Pin Python 3.10-3.12 (avoid 3.14 -- noisy / unsupported wheels).

# Strands agent + tools (local track)
strands-agents
boto3

# Observability: LOCAL PHOENIX is the default backend (Step 3).
arize-phoenix                              # phoenix serve + px.Client span export
openinference-instrumentation-strands-agents
openinference-semantic-conventions         # SpanAttributes / DocumentAttributes for the manual span

# OpenTelemetry SDK + exporters (shared). HTTP exporter for local Phoenix,
# gRPC exporter is used by the AgentCore container path.
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http
opentelemetry-exporter-otlp-proto-grpc

# Tool structured output + experiments + evals
pydantic                                   # RetrievalResult structured tool output
pandas                                     # span dataframe + metrics report
pyarrow                                    # parquet export of spans
openai                                     # LLM-as-judge backend (Step 5)

# Reference architecture diagram (docs/architecture.py)
matplotlib

# ---- Enterprise upgrade path (Arize AX): only needed if TRACE_BACKEND=ax ----
arize-otel

# ---- Bedrock AgentCore deploy (TRACK B): only needed to run agentcore/deploy.py ----
bedrock-agentcore
bedrock-agentcore-starter-toolkit
